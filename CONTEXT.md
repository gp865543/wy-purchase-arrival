# wy-purchase-arrival 业务术语

业务范围、ERP 对接口径与打印契约术语。本仓只读 U8 ERP，不写 U8 库；写入边界限于打印操作与结果记录（沿用根仓 ADR0010）。

## 采购订单（Purchase Order）

U8 `PO_POMain` 记录，对应一次采购交易。本应用仅展示 `cState = 1`（已审核）的订单；其它状态（未审核 / 已关闭）当前不在采购到货收货的范围内。

## 订单明细（Purchase Order Detail）

U8 `PO_PODetails` 记录，每条对应一个物料规格行。标签主字段为订单数量（`iQuantity`）与到货日期（`dArriveDate`）；二维码内容取 `PO_PODetails.ID`（明细行主键）。

## 采购到货收货（本应用）

面向采购员的 PDA 工作场景：从已审核采购订单出发，查看订单物料与数量，按订单数量打印到货标签，供仓库收货与后续质检、上架引用。本应用**不**录入实收数量，**不**生成入库单据——这些动作由"材料到货办理"业务 APP（按根仓应用目录）完成，本仓与之解耦。

## 打印操作（Print Operation）

对一次标签打印的不可变记录，包含订单 ID、明细行 ID 列表、每行的打印份数、操作人与操作时间。打印结束后通过保存打印结果写入每张标签的发送状态（pending / sent / error）。

## 打印次数（Print Count）

按"采购订单 + 订单明细行"维度累计的打印操作成功送出的张数；前端用于卡片已打印次数提示与标签底部"打印次数"展示。

## 到货标签

TSPL/PCX 标签，由 `src/label.ts` 编译、`PurchaseOrderLabel.vue` 渲染预览。包含订单号、到货日期、物料名称、规格、订单数量、二维码（`PO_PODetails.ID`）、操作人、操作时间、打印次数与张序。